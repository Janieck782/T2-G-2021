/**function Simplificar(automatas) {
    var limite = tamañoAlfabeto();
    var cant_est = estados1;
    var cant_alf = tamañoAlfabeto();

    var matrix = [];
    for(let a = 0; a < matrix.length; a++) {
    matrix[i] = new Array();
    }

    console.log(limite);
    //   for(let i = 0; i < automatas.g.length(); i++) {
    //       if(automatas[i].k)
    //   }

    for (let i = 0; i < cant_est; i++) {
    for(let j = 0; j < cant_alf; j++) {
        matrix[i][j] = automatas[i].g;
    }
}*/

